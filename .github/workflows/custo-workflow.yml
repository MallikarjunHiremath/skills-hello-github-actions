name: my-workflow
on: push
jobs:
  my-job:
    environment: Prod Env
    name: my-custom-job
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        uses: actions/checkout@v2
      - name: second-step
        run: echo 'aabra kaa daabra'
      - name: print secret
        run: echo $MALLIKSECRET
      - name: print non secret
        run: echo $MALLIKNONSECRET
  my-job2:
    name: mallikjob
    runs-on: ubuntu-latest
    steps:
      - uses: pullreminders/label-when-approved-action@main
        env:
         approvals: 1
         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
         ADD_LABEL: "approved"
        
    
